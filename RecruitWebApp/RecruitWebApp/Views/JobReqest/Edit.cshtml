@model RecruitWebApp.Models.JobReqest

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>JobReqest</legend>

        @Html.HiddenFor(model => model.JobReqestId)

        <div class="editor-label">
            @Html.LabelFor(model => model.Jobnum)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Jobnum)
            @Html.ValidationMessageFor(model => model.Jobnum)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ApplicantId, "Applicant")
        </div>
        <div class="editor-field">
            @Html.DropDownList("ApplicantId", String.Empty)
            @Html.ValidationMessageFor(model => model.ApplicantId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
